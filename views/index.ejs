<% include ./partials/header %>

<% blogs.forEach(blog => { %>
<h1> <%= blog.title %></h1>
<div>
  <img src="<%= blog.image %>" />
</div>
<p> <%= blog.description %> </p>
<% 
  }
) 
%>

<% include ./partials/footer %>
